# Local-only: heavy backtesting, hyperopt sweeps, and ML training.
# Never installed on the VPS — keeps the server footprint (and bill) minimal.
-r requirements-server.txt
yfinance
fredapi
pyarrow
scikit-learn
lightgbm
shap
pandas-ta
matplotlib
jupyter
pytest
